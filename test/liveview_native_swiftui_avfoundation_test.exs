defmodule LiveviewNativeSwiftuiAvfoundationTest do
  use ExUnit.Case
  doctest LiveviewNativeSwiftuiAvfoundation

  test "greets the world" do
    assert LiveviewNativeSwiftuiAvfoundation.hello() == :world
  end
end
